---
interface PlayProps{
    title: string;
}

const { title = 'BlackJack' } = Astro.props as PlayProps;
---

<section>
    <h1 class="font-bold text-2xl text-primary-900 text-center">{title}</h1>
</section>